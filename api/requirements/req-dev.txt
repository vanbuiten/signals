# Local development dependencies go here
-r req-base.txt

# Debugging
django-debug-toolbar

# Let's also add the testing requirements
-r req-test.txt
django>=2.2.24 # not directly required, pinned by Snyk to avoid a vulnerability
